


spring:
  web:
    resources:
      static-locations: classpath:/static/**

# MyBatis配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-handlers-package: com.zhuxi.userModule.infrastructure.typeHandler

