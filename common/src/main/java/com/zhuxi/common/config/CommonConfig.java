/*
package com.zhuxi.common.config;

import com.zhuxi.common.properties.BCryptProperties;
import com.zhuxi.common.properties.JwtProperties;
import com.zhuxi.common.utils.BCryptUtils;
import com.zhuxi.common.utils.JwtUtils;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

*/
/**
 * @author zhuxi
 *//*

@Configuration
public class CommonConfig {

    @Bean
    public BCryptUtils bCryptUtils(BCryptProperties bCryptProperties) {
        return new BCryptUtils(bCryptProperties);
    }

    @Bean
    public JwtUtils jwtUtils(JwtProperties jwtProperties) {
        return new JwtUtils(jwtProperties);
    }
}
*/
